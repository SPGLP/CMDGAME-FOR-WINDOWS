一个文字游戏专用启动器

本机于不久之前用CMD(没错，就是命令提示符）写过一个游戏（其实就是用 .bat 文件写的一个垃圾文字游戏啦）

但是游戏最后搞定时包括了十几个独立的 .bat 文件 发给同学玩他都麻了，问本机应该从哪个文件开始 而且还有个bug，因为游戏过程中涉及到数据的操作（比如金币的变动），而金币的数量寄存于一个txt文件中， 如果游戏被中断（也就是突然关闭CMD窗口），那么txt文件中的数量不会重置，下次重开游戏时还是这个数字。

所以本机打算把这些文件弄成一个整合的包，再开发一个程序来启动这个包，最好还能控制数据。

控制数据是个难点，如有大佬还望指点指点啦awa。
